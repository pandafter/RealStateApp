services:
  mongo:
    image: mongo:7
    restart: always
    ports: ["27017:27017"]
    volumes: ["mongo_data:/data/db"]
  mongo-express:
    image: mongo-express:1
    restart: always
    ports: ["8081:8081"]
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
volumes:
  mongo_data:
